@model ServiceTracker.Models.InterestIndexViewModel

<h1>Interests for @Model.InterestYear Academic Year</h1>

<hr>
<div>
    <h2>Interest in Departmental Committe(s)</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Committe</th>
                <th>Preferance Order</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var interest in Model.interests)
            {
                <tr>
                    <td>@interest.Committee.Name</td>
                    <td>@interest.PreferenceOrder</td>
                    <td>
                        @if(@interest.PreferenceOrder == 1)
                        {
                            <i class="fa-solid fa-circle-arrow-up"></i>
                        } else 
                        {
                            <a asp-action="MoveUp" asp-route-id="@interest.Id"><i class="fa-solid fa-circle-arrow-up"></i></a>
                        } 
                        |
                        @if(@interest.PreferenceOrder == Model.interests.Last().PreferenceOrder)
                        {
                            <i class="fa-solid fa-circle-arrow-down"></i>
                        } else 
                        {
                            <a asp-action="MoveDown" asp-route-id="@interest.Id"><i class="fa-solid fa-circle-arrow-down"></i></a>
                        }                       
                        | <a asp-action="Delete" asp-route-id="@interest.Id" onclick="return confirm('Are you sure you want to delete this interest?')"><i class="fa-solid fa-trash-can"></i></a>
                    </td>
                </tr>                
            }
        </tbody>
    </table>
    <a asp-action="New" class="btn btn-primary">Add Interest</a>
</div>
<div>
    <hr>
    <h2>Current Plant Sciences Committee(s)</h2>
    @if(Model.currentAssignments.Count > 0)
    {
        <ul>
             @foreach (var com in Model.currentAssignments)
            {
                <li>@com.Name</li> 
            }
        </ul>
    } else
    {
        <span>No current assignments for next year.</span>
    }
    
</div>
@* <hr>
<div>
    <h2>Current Appointment & Committements</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Start Year</th>
                <th>End Year</th>
                <th>Location/Level</th>
                <th>Position</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var commitment in Model.commitments)
            {
                <tr>
                    <td>@commitment.Title</td>
                    <td>@commitment.startYear</td>
                    <td>@commitment.endYear</td>
                    <td>@commitment.Location</td>
                    <td>@commitment.Description</td>    
                    <td><a asp-action="Edit" asp-controller="Commitment" asp-route-id="@commitment.Id"><i class="fa-solid fa-pen"></i></a> | <a asp-action="Delete" asp-controller="Commitment" asp-route-id="@commitment.Id" onclick="return confirm('Are you sure you want to delete this commitment?')"><i class="fa-solid fa-trash-can"></i></a></td>                
                </tr>
            }
        </tbody>
    </table>
    <a asp-action="New" asp-controller="Commitment" class="btn btn-primary">Add Appointment or Committment</a>
    <h2>Past Appointment & Committements (in case they've been extended)</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Start Year</th>
                <th>End Year</th>
                <th>Location/Level</th>
                <th>Position</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var commitment in Model.pastCommitments)
            {
                <tr>
                    <td>@commitment.Title</td>
                    <td>@commitment.startYear</td>
                    <td>@commitment.endYear</td>
                    <td>@commitment.Location</td>
                    <td>@commitment.Description</td>    
                    <td><a asp-action="Edit" asp-controller="Commitment" asp-route-id="@commitment.Id"><i class="fa-solid fa-pen"></i></a> | <a asp-action="Delete" asp-controller="Commitment" asp-route-id="@commitment.Id" onclick="return confirm('Are you sure you want to delete this commitment?')"><i class="fa-solid fa-trash-can"></i></a></td>                
                </tr>
            }
        </tbody>
    </table>
</div> *@


